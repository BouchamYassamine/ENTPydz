# Configuration du serveur Express
PORT=5000
NODE_ENV=development

# Clé secrète pour la génération des tokens JWT
JWT_SECRET=votre_cle_secrete_super_securisee_entp_2026
JWT_EXPIRES_IN=8h

# Configuration de la base de données (Exemple avec MySQL via Sequelize)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=mot_de_passe
DB_NAME=entp_transfert_db
DB_PORT=3306
DB_DIALECT=mysql
