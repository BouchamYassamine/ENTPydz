// Configuration optionnelle de Passport.js pour la gestion des stratégies d'authentification.
// Vous pouvez configurer ici PassportJWT ou PassportLocal.

export const configurePassport = (passport) => {
  // Configurer les stratégies d'authentification ici
};
